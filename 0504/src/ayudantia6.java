import java.util.*;
import java.io.*;

public class ayudantia6 {
    static void divideAndConquer(int[] x, int l, int r){
        int mid = l + r / 2;
        if(){

        }
        divideAndConquer(x, l, mid-1);
        divideAndConquer(x, mid+1, r);
    }
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int N = input.nextInt();
        int[] x = 
    }    
}
